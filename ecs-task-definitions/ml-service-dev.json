{
  "family": "medic-ml-service-dev",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["EC2"],
  "cpu": "3584",
  "memory": "14336",
  "executionRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::${AWS_ACCOUNT_ID}:role/ecsTaskRole",
  "placementConstraints": [
    {
      "type": "memberOf",
      "expression": "attribute:environment == dev"
    }
  ],
  "containerDefinitions": [
    {
      "name": "medic-ml-container-dev",
      "image": "${ECR_ML_SERVICE}:dev",
      "portMappings": [
        {
          "containerPort": 8001,
          "protocol": "tcp"
        }
      ],
      "essential": true,
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/medic-ml-dev",
          "awslogs-region": "ap-northeast-2",
          "awslogs-stream-prefix": "ecs"
        }
      },
      "environment": [
        {
          "name": "ENVIRONMENT",
          "value": "dev"
        },
        {
          "name": "MODEL_NAME",
          "value": "monologg/kobert"
        },
        {
          "name": "DEVICE",
          "value": "cuda"
        }
      ],
      "resourceRequirements": [
        {
          "type": "GPU",
          "value": "1"
        }
      ]
    }
  ]
}
